runtime: java
env: flex
runtime_config:
  jdk: java11
env_variables:
  SPRING_PROFILES_ACTIVE: "cloud-gcp"
handlers:
  - url: /.*
    script: this field is required, but ignored
manual_scaling:
  instances: 1